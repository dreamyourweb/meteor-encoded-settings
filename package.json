{
  "name": "meteor-encoded-settings",
  "version": "1.0.0",
  "description": "Load Meteor settings from URI encoded environment variable",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel index.js -d lib",
    "prepublish": "npm run compile"
  },
  "keywords": [
    "meteor"
  ],
  "author": "Andres Lamont",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0"
  }
}
